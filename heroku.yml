setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    RAILS_ENV: development
release:
  command:
    - ./deployment-tasks.sh
  image: worker
run:
  web: node start